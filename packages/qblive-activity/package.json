{
  "name": "@qbsheet/qblive-activity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The compact ActivityKit broadcast shard state shared by the push gateway, the iOS Live Activity, and the payload-size measurements",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest --run tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
