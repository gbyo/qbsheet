# Generated. `dist` and the two report directories are build and test output; the lockfile is npm's
# to format. Prose is left alone on purpose: the wiki pages are synced from and back to GitHub, and
# a formatter that rewrapped them would make every sync a diff.
dist/
**/target/
coverage/
playwright-report/
test-results/
.stryker-tmp/
package-lock.json

# Wrangler's local dev bundle. Generated, and not ours to format.
**/.wrangler/
apps/director/src-tauri/assets/live-web/
