/** Back-compat shim: the Rounds workspace owns the day sequence and round operation. */
export { RoundsView as ScheduleView } from './RoundsView';
export { scheduleTimeZoneOptions } from './RoundsView';
